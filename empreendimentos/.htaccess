# Bloqueia acesso direto apenas à pasta data/ (JSON sensível)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^data/.*$ - [F,L]
</IfModule>

# Bloqueia acesso direto a arquivos JSON
<FilesMatch "\.json$">
  Order allow,deny
  Deny from all
</FilesMatch>
